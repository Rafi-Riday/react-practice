const user = {
    id: 69,
    address: {
        first: 'Chattogram',
        second: 'Navy Sailors Colony 1',
    }
}

// put an ? after that part to be checked if is available
const address = user.add_ress?.first;
console.log(address);
